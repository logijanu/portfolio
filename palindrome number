class Solution {
    public boolean isPalindrome(int x) {
        int r,num=x,n=0;
        if(num<0) return false;
        while(num!=0){
            r = num %10;
            n=(n*10)+r;
            num=num/10;
        }
        return (n==x);
    }
}
